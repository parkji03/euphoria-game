# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development:
  secret_key_base: 9173b4b7cbdefff64a0793b9cc123267cf63c5824b53b10cfd89d38d4f9e7d3a876ebe1f8d1918d9ad41bc3fbb95da132387260fca7ad6b1cd9d1767a4336570

  # Facebook API and Google API
  google_api_id: <%= ENV["google_api_id"] %>
  google_secret: <%= ENV["google_secret"] %>
  facebook_api_id: <%= ENV["facebook_api_id"] %>
  facebook_secret: <%= ENV["facebook_secret"] %>


# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  google_api_id: <%= ENV["GOOGLE_API_ID"] %>
  google_secret: <%= ENV["GOOGLE_SECRET"] %>
  facebook_api_id: <%= ENV["FACEBOOK_API_ID"] %>
  facebook_secret: <%= ENV["FACEBOOK_SECRET"] %>
